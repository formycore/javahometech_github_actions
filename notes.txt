name: Hello World
on: workflow_dispatch
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Build step
              run: echo "This is build step"
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Testing step
              run: echo "This is test step"
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy step
              run: echo "This is deploy step"
---------------------------
sequence


name: Hello World
on: workflow_dispatch
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Build step
              run: echo "This is build step"
    test:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - name: Testing step
              run: echo "This is test step"
    deploy:
        runs-on: ubuntu-latest
        needs: test
        steps:
            - name: Deploy step
              run: echo "This is deploy step"

-------------------------------------------------------------------
push and pull triggers works
# push and pull triggers from any branch
name: Hello World
# workflow for only specific branches, when code changes are pushed to specific branches then only the workflow triggers
on:
    push:
        branches:
            - main
            - develop
            - 'release/**'

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Build step
              run: echo "A Demo Build"
- git branch feature/pr_demo
- git checkout feature/pr_demo
- create a pr request 
- this workflow will start
- there are some types
        -assigned
        - unassigned
        - labeled
        - unlabeled
        - opened
        - edited
        - closed
        - reopened
        - synchronize
        - converted_to_draft
        - locked
        - unlocked
        - enqueued
        - dequeued
        - milestoned
        - demilestoned
        - ready_for_review
        - review_requested
        - review_request_removed
        - auto_merge_enabled
        - auto_merge_disabled
------------------------------------------------------------------------------------------------------------------------
# variables 
- login to github
- go to settings
- security and Quality
- secrets and variables
- actions
- new repository variable
