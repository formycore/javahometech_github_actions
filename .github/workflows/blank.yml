# push and pull triggers from any branch
name: Hello World
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Build step
              run: echo "This is coming from feature pr_demo branch"
